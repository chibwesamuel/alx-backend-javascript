/**
 * Will print a given message to STDOUT.
 * @param {string} message - The message to be printed.
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
